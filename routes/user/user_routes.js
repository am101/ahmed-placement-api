module.exports = function(app, db) {
    app.get('/users', (req, res) => {
        const user = {
            username: String,
            email: String,
            status: String,
            
        }
    });
}